problem #6:  run command without any output 

sol:  an ugly(but working solution):
i created a function in .bashrc file and trap it.

func(){
  /bin/bash
}
trap 'func' DEBUG

- in this case i'm assuming there is /bin/bash present.
